{% extends '::base.html.twig' %}

{% block body -%}
    <h1>VideoAcervo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Director</th>
                <th>Composer</th>
                <th>Interpret</th>
                <th>Producer</th>
                <th>Url</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('videoacervo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.director }}</td>
                <td>{{ entity.composer }}</td>
                <td>{{ entity.interpret }}</td>
                <td>{{ entity.producer }}</td>
                <td>{{ entity.url }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('videoacervo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('videoacervo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('videoacervo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
