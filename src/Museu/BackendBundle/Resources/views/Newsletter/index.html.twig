{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Newsletter</h1>

    <table class="table table-striped table-bordered table-advance table-hover">
        <thead>
            <tr>
                <th>Email</th>
                <th style="width:130px;"></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.email }}</td>
                <td>
                    <a class="btn default btn-xs green-stripe" href="{{ path('newsletter_show', { 'id': entity.id }) }}">
                         Ver
                    </a>
                    <a class="btn default btn-xs green-stripe" href="{{ path('newsletter_edit', { 'id': entity.id }) }}">
                         Editar
                    </a>
                
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('newsletter_new') }}">
        <button type="button" class="btn btn-default">Criar Nova</button>
    </a>
    {% endblock %}
